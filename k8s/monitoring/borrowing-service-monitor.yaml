apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: borrowing-service-monitor
  labels:
    release: kube-monitoring
spec:
  selector:
    matchLabels:
      app: borrowing-service
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
  namespaceSelector:
    matchNames:
      - default
